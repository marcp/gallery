<% if @painting.new_record? %>
  alert("Failed to upload file: <%= j @painting.errors.full_messages.join(', ').html_safe %>");
<% else %>
$("#paintings").append("<%= j render(@painting) %>");
<% end %>